# clips/audio/

Stage 4A/4B output goes here: `voice_final.wav` (final Voice asset) and
`voice_alignment.json` (waveform-based word/segment timestamps from forced
alignment). Not checked into git — see `.gitignore`.

This example project has no Voice yet (`edit_plan.json` is at ESTIMATED
grade), so this directory is otherwise empty.
