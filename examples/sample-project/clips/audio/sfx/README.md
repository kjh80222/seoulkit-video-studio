# clips/audio/sfx/

Stage 3 Optional Source Audio (reference/production source only, never the
final mix — Stage 3 manual, ch. 10). Referenced from `edit_plan.json`'s
`audio_layers.sfx_source.clips[]`, each resolved to `adopted` or
`discarded` before the plan reaches Stage 5. Not checked into git.
